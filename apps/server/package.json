{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "db:migrate": "bun run --cwd ../../packages/db db:migrate",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs",
    "test:http": "test -n \"$DATABASE_URL\" || (echo 'DATABASE_URL must point to an isolated PostgreSQL database' >&2; exit 1); bun test --timeout 120000 test/http.test.ts",
    "test:storage": "bun test test/storage.test.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@onlyoffice/auth": "workspace:*",
    "@onlyoffice/db": "workspace:*",
    "@onlyoffice/env": "workspace:*",
    "@sinclair/typebox": "^0.34.52",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "elysia": "^1.4.29",
    "fflate": "^0.8.3",
    "saxes": "^6.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@onlyoffice/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
